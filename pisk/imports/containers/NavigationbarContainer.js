import {composeWithTracker} from 'react-komposer';
import {Meteor} from 'meteor/meteor';

const NavigationbarComposer = (context, onData) => {


};

export default composeWithTracker(NavigationbarComposer)(Navigationbar);
